pipeline{
	agent any
	tools{
		maven "maven3.9.9"
	}
	environment{
	NVM_HOME = ''
	}
	stages{
		stage("build"){
			steps{
				echo "${PATH} ${HOME} ${USER}"
			}
		}
		stage("Sonarqube analysis"){
			steps{
				sh "which node"
			}
		}
		stage("deploy"){
			steps{
				echo "Deploying the apps"
			}
		}
	}
}